#include "image.h"

#include "stream/file_stream.h"

namespace scythe {

	bool Image::SaveTiff(const char *filename)
	{
		assert(!"not implemented yet");
		return true;
	}
	bool Image::LoadTiff(const char *filename)
	{
		assert(!"not implemented yet");
		return true;
	}
	bool Image::LoadFromBufferTiff(const u8* buffer, size_t length)
	{
		assert(!"not implemented yet");
		return true;
	}

} // namespace scythe